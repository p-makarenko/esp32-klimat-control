# 📊 Налаштування Google Sheets для ESP32 Klimat

## ✅ Що вже зроблено:

1. ✅ **Виправлено проблему з датою 1970** - NTP вже налаштований, використовується Unix timestamp
2. ✅ **Прибрано заголовки з CSV** - економія 50% пам'яті флеш
3. ✅ **Створено систему синхронізації** - автоматична відправка кожні 30 хв
4. ✅ **Додано команди** - `sheets-sync`, `sheets-stats`
5. ✅ **Інтеграція з data_logger** - використовує нову систему логування

## 🚀 Швидкий старт (5 кроків):

### Крок 1: Створіть Google Таблицю
1. Відкрийте [Google Sheets](https://sheets.google.com)
2. Створіть нову таблицю
3. Назвіть її: "ESP32 Klimat Data"

### Крок 2: Додайте заголовки (перший рядок)
1. Виділіть комірку **A1** в Google Sheets
2. Скопіюйте цей рядок (він розділений табуляцією):
   ```
   Дата і час	Теплоносій °C	Кімната °C	BME280 °C	Вологість %	Насос %	Вентилятор %	Витяжка %	Режим
   ```
3. Натисніть **Ctrl+V** - Google Sheets автоматично розкладе по стовпчиках

**Результат:**

| A | B | C | D | E | F | G | H | I |
|---|---|---|---|---|---|---|---|---|
| Дата і час | Теплоносій °C | Кімната °C | BME280 °C | Вологість % | Насос % | Вентилятор % | Витяжка % | Режим |

### Крок 3: Створіть Google Apps Script
1. В таблиці: **Розширення** → **Apps Script**
2. Видаліть весь код
3. Вставте код з файлу **GOOGLE_APPS_SCRIPT.md** (функція `doPost`)
4. Натисніть **Зберегти** (💾)

### Крок 4: Опублікуйте Web App
1. **Deploy** → **New deployment**
2. Тип: **Web app**
3. Налаштування:
   - Execute as: **Me**
   - Who has access: **Anyone**
4. **Deploy**
5. **Скопіюйте URL** (виглядає так: `https://script.google.com/macros/s/AKfycbz.../exec`)

### Крок 5: Налаштуйте ESP32
1. Відкрийте файл `src/google_sheets_sync.h`
2. Знайдіть рядок 13:
   ```cpp
   #define GOOGLE_SCRIPT_URL "https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec"
   ```
3. Замініть `YOUR_DEPLOYMENT_ID` на ваш URL з кроку 4
4. Збережіть файл
5. Скомпілюйте і завантажте прошивку:
   ```bash
   pio run -t upload
   ```

## 🎯 Як працює система:

### Автоматична синхронізація:
- **Кожні 30 хвилин** - якщо є 10+ нових записів
- **Щодня о 23:59** - незалежно від кількості даних
- **Без дублювання** - зберігає lastSentTimestamp в NVS

### Ручна синхронізація:
- **Веб-інтерфейс**: Введіть команду `sheets-sync`
- **Serial Monitor**: Введіть команду `sheets-sync`

### Перевірка статистики:
- **Команда**: `sheets-stats`
- **Показує**: кількість відправлених записів, помилки, останній timestamp

## 📁 Структура даних:

### Що відправляє ESP32 (CSV без заголовків):
```csv
1767875458,63.4,28.1,24.3,67.1,0,9,0,0
1767875518,63.9,27.2,24.3,68.1,0,9,0,0
```

### Що з'являється в Google Sheets:
| Дата і час | Теплоносій °C | Кімната °C | BME280 °C | Вологість % | Насос % | Вентилятор % | Витяжка % | Режим |
|------------|---------------|------------|-----------|-------------|---------|--------------|-----------|-------|
| 2026-01-08 15:30:58 | 63.4 | 28.1 | 24.3 | 67.1 | 0 | 9 | 0 | 0 |
| 2026-01-08 15:31:58 | 63.9 | 27.2 | 24.3 | 68.1 | 0 | 9 | 0 | 0 |

### Пояснення полів:
- **Режим**: 0=AUTO, 1=MANUAL, 2=FORCE, 3=EMERGENCY

## ⚙️ Налаштування (необов'язково):

У файлі `src/google_sheets_sync.h` можна змінити:

```cpp
#define SYNC_INTERVAL_MS 1800000          // 30 хвилин (змініть якщо потрібно)
#define SYNC_MIN_NEW_RECORDS 10           // Мінімум нових записів
#define SYNC_DAILY_HOUR 23                // Година щоденної синхронізації
#define SYNC_DAILY_MINUTE 59              // Хвилина щоденної синхронізації
#define SYNC_BATCH_SIZE 50                // Розмір пакету (записів за раз)
```

## 🧪 Тестування:

### Тест 1: Apps Script
1. В Apps Script натисніть **Виконати** → **testDoPost**
2. Дозвольте доступ (при першому запуску)
3. Перевірте чи з'явились 2 тестові рядки в таблиці

### Тест 2: Ручна синхронізація
1. Відкрийте Serial Monitor або веб-інтерфейс
2. Введіть: `sheets-sync`
3. Очікуваний результат:
   ```
   📤 Початок синхронізації з Google Sheets...
   📊 Знайдено X нових записів для відправки
   ✅ Синхронізація завершена: X успішно, 0 помилок
   ```
4. Перевірте таблицю - дані мають з'явитись

### Тест 3: Статистика
1. Введіть: `sheets-stats`
2. Очікуваний результат:
   ```
   📊 СТАТИСТИКА GOOGLE SHEETS СИНХРОНІЗАЦІЇ
   Останній відправлений timestamp: 1767875458
   Всього відправлено за сесію: 50 записів
   Невдалих синхронізацій: 0
   ```

## 🔧 Усунення проблем:

### Помилка 403 (Forbidden)
- Перевірте: "Who has access: **Anyone**" в налаштуваннях Deploy
- Спробуйте створити новий Deployment

### Помилка 302 (Redirect)
- Це нормально! Скрипт працює
- Перевірте таблицю - дані мають бути

### Помилка timeout
- Зменшіть `SYNC_BATCH_SIZE` до 30-40
- Перевірте інтернет з'єднання ESP32

### Дані не з'являються
- Перевірте URL в `google_sheets_sync.h`
- Запустіть `testDoPost()` в Apps Script
- Перевірте Serial Monitor на предмет помилок
- Перевірте що таблиця НЕ захищена від редагування

### NTP не синхронізується (дата 1970)
- Перезавантажте ESP32
- Зачекайте 1-2 хвилини для синхронізації
- Перевірте підключення до інтернету

## 📊 Економія пам'яті:

### Було (з заголовками):
```csv
timestamp,tempCarrier,tempRoom,tempBME,humidity,pumpPower,fanPower,extractorPower,mode
1767875458,63.4,28.1,24.3,67.1,0,9,0,0
```
**Розмір**: ~80 байт/запис

### Стало (без заголовків):
```csv
1767875458,63.4,28.1,24.3,67.1,0,9,0,0
```
**Розмір**: ~40 байт/запис

**Економія**: 50% пам'яті! 🎉

## 🔐 Безпека:

- ✅ Дані передаються через HTTPS
- ✅ URL публічний, але таблиця приватна
- ✅ Тільки ви маєте доступ до таблиці
- ✅ Дані зберігаються тільки при підключенні Wi-Fi
- ✅ Локальна робота продовжується навіть без інтернету

## 📞 Підтримка:

Якщо виникли проблеми:
1. Перевірте Serial Monitor - там детальні повідомлення
2. Запустіть `sheets-stats` для діагностики
3. Перевірте Google Apps Script logs (View → Logs)
4. Детальна документація в файлі **GOOGLE_APPS_SCRIPT.md**

---

**Успіхів! 🚀**
