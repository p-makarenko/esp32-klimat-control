# Відстеження використання токенів

Цей файл містить інформацію про витрати токенів при роботі з AI асистентом.

## Формат запису

```
Дата | Час | Токени використано | Опис змін
```

---

## Історія змін

### 2026-01-03 21:59

**Токенів використано:** 60,775

**Опис змін:**
- Виправлено критичні баги в системі перемикання режимів веб-інтерфейсу
- Додано функцію updateModeButtons() на головну сторінку з перевіркою існування
- Видалено кнопки форсажу та аварії з налаштувань (автоматичні режими)
- Додано інформаційне повідомлення про автоматичні режими ФОРСАЖ (<20°C) та АВАРІЯ (<18°C)
- Виправлено логіку advancedLogicTask: змінено умову з `!manualMode && !forceMode && !emergencyMode` на `!manualMode`
- Додано скидання режимів force/emergency при нормалізації температури
- Додано взаємовиключення між force та emergency режимами
- Виправлено колізію парсингу команди "auto" (розпізнавалась як "a0")
- Переміщено обробку режимних команд ПЕРЕД компактними командами
- Додано автоматичне перемикання в ручний режим при командах пристроїв (a50, b40, c30, pump, fan, extractor)
- Додано індикатор "[РУЧНИЙ]" в повідомленнях відповіді на команди пристроїв
- Покращено UI: додано анімації, градієнти, pulse-ефект для активних кнопок режимів

**Змінені файли:**
- `src/web_interface.cpp` (1783 рядки)
- `src/advanced_climate_logic.cpp` (824 рядки)

**Результат:** Всі функції тестовані та підтверджені користувачем як працюючі

---

### Наступні записи будуть додані тут

